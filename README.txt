Example command:

    ./main.py --name "Dave Young" --email dyoung@redhat.com ruyang@redhat.com \
    --year 2016 --month 7 --pipermail "kexec" --hyperkitty "kexec" \
    --rh_internal "kexec-kdump-list" --spinics "linux-initramfs" lkml

--pipermail "kexec" means kexec upstream mailing list
--hyperkitty "kexec" means fedora's kexec list
--spinics is one of the archives of vger.kernel.org's lists

#TODO
#1. output result at last
2. statics the number of the same name reply
#3. process
4. the location config file
